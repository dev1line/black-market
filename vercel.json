{
  "rootDirectory": ".",
  "buildCommand": "yarn build && ls -R dist",
  "outputDirectory": "dist/apps/black-market",
  "installCommand": "yarn install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
